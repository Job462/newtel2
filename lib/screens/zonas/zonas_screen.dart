import 'package:flutter/material.dart';

class ZonasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Zonas'),
      ),
      body: Center(
        child: Text('Pantalla de Zonas'),
      ),
    );
  }
}
